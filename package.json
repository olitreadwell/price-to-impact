{
  "name": "price-to-impact",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter='@price-to-impact/web' dev",
    "build": "bun --filter='@price-to-impact/web' build",
    "lint": "bun --filter='*' lint"
  }
}
