{
  "name": "@price-to-impact/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'no lint'",
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run",
    "build": "bun run build.ts",
    "package": "bun scripts/package.ts"
  },
  "dependencies": {
    "@price-to-impact/bookmarklet": "workspace:*",
    "@price-to-impact/charities": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.1",
    "@types/chrome": "^0.3.0",
    "@types/node": "^26",
    "happy-dom": "^20",
    "typescript": "^7",
    "vitest": "^5.0.1"
  }
}
